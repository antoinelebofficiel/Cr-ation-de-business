# Site Clartéo

Statique. Zéro build.

| Fichier | Rôle |
|---|---|
| `index.html` | Google / bouche-à-oreille. **Jamais une pub Meta.** |
| `lp.html?a=` | Landing. **Destination des pubs Meta.** |
| `politique.html` | URL obligatoire dans l’Instant Form Meta. |

Pack ads : `../lancer-meta/`.

Pixel, tél `06 40 09 75 75`, WhatsApp : `js/config.js`. Events : PageView, ViewContent, Lead.

Local : `ouvrir.sh` / `ouvrir.bat` — port **8000**.
